# SUMM-Cli Implementation Plan

> 历史说明：本文档记录 2026-04-21 的 Bash 插件式实现计划。当前活跃运行时已经迁移到 npm/Node.js 单文件 CLI，后续维护请以 `CLAUDE.md`、`README.md` 和 `docs/plans/2026-06-09-npm-global-cli.md` 为准。

> **For agentic workers:** REQUIRED SUB-SKILL: Use summ:subagent-driven-development (recommended) or summ:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a plugin-based Bash CLI framework that unifies API tooling under `summ`, with auto-discovered plugins, unified config, and TOON output.

**Architecture:** Single `bin/summ` entry point sources shared libraries (`lib/core.sh`, `lib/output.sh`), auto-discovers plugins in `plugins/<name>/plugin.sh`, and routes subcommands to plugin functions. Config lives in `~/.summ/.env` with env var override. Output defaults to TOON, switchable to JSON via `--json`.

**Tech Stack:** Bash 5.2, curl, jq, Node.js (npx @toon-format/cli for TOON output)

---

## File Structure

| File | Responsibility |
|------|---------------|
| `bin/summ` | Entry point: resolve dir, source libs, parse args, route commands |
| `lib/core.sh` | Core functions: `summ_error`, `summ_config_get`, `summ_config_set`, plugin discovery |
| `lib/output.sh` | Output formatting: `summ_output` (TOON/JSON), `--json` flag handling |
| `plugins/weather/plugin.sh` | Weather plugin: `plugin_info`, `cmd_realtime`, `cmd_daily`, `cmd_hourly`, `cmd_minutely` |
| `plugins/weather/SKILL.md` | Claude Agent Skill definition for weather |
| `plugins/amap-geo/plugin.sh` | Geocoding plugin: `plugin_info`, `cmd_default` |
| `plugins/amap-geo/SKILL.md` | Claude Agent Skill definition for amap-geo |
| `.env.example` | Config template with all keys documented |
| `install.sh` | System installer: symlink, npm toon, config init, skill symlinks |

---

### Task 1: Core library — error handling and config

**Files:**
- Create: `lib/core.sh`

This task creates the shared function library that all other code depends on.

- [ ] **Step 1: Create `lib/core.sh` with error handling and config functions**

```bash
#!/bin/bash
# lib/core.sh — SUMM-Cli core functions

# Resolve SUMM_HOME: directory where bin/summ lives (handles symlinks)
_resolve_summ_home() {
    local source="${BASH_SOURCE[0]}"
    while [[ -L "$source" ]]; do
        local dir="$(cd -P "$(dirname "$source")" && pwd)"
        source="$(readlink "$source")"
        [[ "$source" != /* ]] && source="$dir/$source"
    done
    SUMM_HOME="$(cd -P "$(dirname "$source")/.." && pwd)"
}

# Print error to stderr and exit 1
summ_error() {
    echo "[summ] 错误: $1" >&2
    exit 1
}

# Load config from ~/.summ/.env (env vars take priority)
_summ_load_config() {
    SUMM_ENV_FILE="${HOME}/.summ/.env"
    if [[ -f "$SUMM_ENV_FILE" ]]; then
        set -a
        # shellcheck disable=SC1090
        while IFS='=' read -r key value; do
            [[ -z "$key" || "$key" == \#* ]] && continue
            # Only set if not already in environment
            if [[ -z "${!key}" ]]; then
                export "$key=$value"
            fi
        done < "$SUMM_ENV_FILE"
        set +a
    fi
    SUMM_CONFIG_LOADED=true
}

# Get config value: env var > .env file > default
summ_config_get() {
    local key="$1"
    local default="${2:-}"
    if [[ -n "${!key}" ]]; then
        echo "${!key}"
    elif [[ -f "${SUMM_ENV_FILE:-$HOME/.summ/.env}" ]]; then
        local file_val
        file_val="$(grep "^${key}=" "$SUMM_ENV_FILE" 2>/dev/null | head -1 | cut -d= -f2-)"
        echo "${file_val:-$default}"
    else
        echo "$default"
    fi
}

# Set config value in ~/.summ/.env
summ_config_set() {
    local key="$1"
    local value="$2"
    local env_file="${HOME}/.summ/.env"

    [[ -z "$key" ]] && summ_error "config set 需要 key 参数"
    [[ -z "$value" ]] && summ_error "config set 需要 value 参数"

    mkdir -p "$(dirname "$env_file")"

    if grep -q "^${key}=" "$env_file" 2>/dev/null; then
        sed -i "s|^${key}=.*|${key}=${value}|" "$env_file"
    else
        echo "${key}=${value}" >> "$env_file"
    fi
}

# Discover all plugin directories
_summ_discover_plugins() {
    local plugins_dir="${SUMM_HOME}/plugins"
    if [[ ! -d "$plugins_dir" ]]; then
        return
    fi
    for plugin_dir in "$plugins_dir"/*/; do
        [[ -f "${plugin_dir}plugin.sh" ]] && echo "${plugin_dir}"
    done
}

# Initialize core
_resolve_summ_home
```

- [ ] **Step 2: Verify file loads without errors**

Run: `bash -n lib/core.sh && echo "SYNTAX OK"`
Expected: `SYNTAX OK`

- [ ] **Step 3: Manual smoke test**

Run:
```bash
source lib/core.sh && \
echo "SUMM_HOME=$SUMM_HOME" && \
summ_config_get NONEXISTENT_KEY fallback_value && \
summ_error "test" || echo "error exit OK (exit $?)"
```
Expected: Prints SUMM_HOME path, `fallback_value`, then error message and exit 1.

- [ ] **Step 4: Initialize git repo and commit**

```bash
cd /data/dev/SUMM-Cli
git init
git add lib/core.sh
git commit -m "feat: add core library with config and error handling"
```

---

### Task 2: Output formatting library

**Files:**
- Create: `lib/output.sh`

This task creates the output module that handles TOON/JSON formatting.

- [ ] **Step 1: Create `lib/output.sh`**

```bash
#!/bin/bash
# lib/output.sh — SUMM-Cli output formatting

# Format JSON data for display
# Uses TOON by default, JSON when SUMM_OUTPUT_JSON=true
summ_output() {
    local json_data="$1"

    if [[ -z "$json_data" ]]; then
        return
    fi

    if [[ "$SUMM_OUTPUT_JSON" == "true" ]]; then
        echo "$json_data" | jq .
    elif command -v toon &>/dev/null; then
        echo "$json_data" | toon
    elif command -v npx &>/dev/null; then
        echo "$json_data" | npx @toon-format/cli 2>/dev/null
    else
        echo "$json_data" | jq .
        echo "[提示] 安装 toon 以获得更好的输出: npm install -g @toon-format/cli" >&2
    fi
}

# Parse --json flag from arguments, strip it out
# Sets SUMM_OUTPUT_JSON and returns cleaned args in SUMM_CLEAN_ARGS
summ_parse_output_flag() {
    SUMM_OUTPUT_JSON="false"
    SUMM_CLEAN_ARGS=()

    local args=("$@")
    local i=0
    while [[ $i -lt ${#args[@]} ]]; do
        case "${args[$i]}" in
            --json)
                SUMM_OUTPUT_JSON="true"
                ;;
            *)
                SUMM_CLEAN_ARGS+=("${args[$i]}")
                ;;
        esac
        ((i++))
    done
}
```

- [ ] **Step 2: Verify syntax**

Run: `bash -n lib/output.sh && echo "SYNTAX OK"`
Expected: `SYNTAX OK`

- [ ] **Step 3: Smoke test output formatting**

Run:
```bash
source lib/output.sh && \
summ_parse_output_flag "daily" "--json" && \
echo "JSON mode: $SUMM_OUTPUT_JSON" && \
echo "Clean args: ${SUMM_CLEAN_ARGS[*]}" && \
echo '{"test":true}' | summ_output '{"test":true}' | head -5
```
Expected: `JSON mode: true`, `Clean args: daily`, formatted JSON output.

- [ ] **Step 4: Commit**

```bash
git add lib/output.sh
git commit -m "feat: add output formatting library with TOON/JSON support"
```

---

### Task 3: Entry point and command routing

**Files:**
- Create: `bin/summ`

This task creates the main CLI entry point that ties everything together.

- [ ] **Step 1: Create `bin/summ`**

```bash
#!/bin/bash
# bin/summ — SUMM-Cli entry point

set -e

# Source libraries
SELF_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SELF_DIR}/../lib/core.sh"
source "${SELF_DIR}/../lib/output.sh"

# Load config
_summ_load_config

# Parse global flags (--json)
summ_parse_output_flag "$@"
set -- "${SUMM_CLEAN_ARGS[@]}"

# Built-in commands
_cmd_help() {
    if [[ -n "$1" ]]; then
        _cmd_help_plugin "$1"
        return
    fi

    echo "SUMM-Cli — 统一 API 工具集"
    echo ""
    echo "用法: summ <插件> [子命令] [选项] [参数]"
    echo "      summ <内置命令> [参数]"
    echo ""
    echo "内置命令:"
    echo "  help [插件]      显示帮助信息"
    echo "  list             列出已安装插件"
    echo "  init             初始化配置"
    echo "  config get KEY   查看配置"
    echo "  config set K V   设置配置"
    echo ""
    echo "全局选项:"
    echo "  --json           输出 JSON 格式"
    echo ""
    echo "可用插件:"

    for plugin_dir in $(_summ_discover_plugins); do
        local name="$(basename "$plugin_dir")"
        source "${plugin_dir}plugin.sh"
        local info="$(plugin_info)"
        local desc="$(echo "$info" | cut -d: -f2)"
        printf "  %-15s %s\n" "$name" "$desc"
    done
}

_cmd_help_plugin() {
    local plugin_name="$1"
    local plugin_file="${SUMM_HOME}/plugins/${plugin_name}/plugin.sh"

    [[ ! -f "$plugin_file" ]] && summ_error "未知插件: $plugin_name"

    source "$plugin_file"
    local info="$(plugin_info)"
    local desc="$(echo "$info" | cut -d: -f2)"
    local subcmds="$(echo "$info" | cut -d: -f3 | tr '|' ', ')"

    echo "summ ${plugin_name} — ${desc}"
    echo ""
    echo "子命令: ${subcmds}"
    echo ""

    if declare -f plugin_help &>/dev/null; then
        plugin_help
    fi
}

_cmd_list() {
    for plugin_dir in $(_summ_discover_plugins); do
        local name="$(basename "$plugin_dir")"
        source "${plugin_dir}plugin.sh"
        local info="$(plugin_info)"
        local desc="$(echo "$info" | cut -d: -f2)"
        printf "%-15s %s\n" "$name" "$desc"
    done
}

_cmd_init() {
    local summ_dir="${HOME}/.summ"
    mkdir -p "$summ_dir"

    if [[ ! -f "${summ_dir}/.env" ]]; then
        cp "${SUMM_HOME}/.env.example" "${summ_dir}/.env"
        echo "已创建配置文件: ${summ_dir}/.env"
    else
        echo "配置文件已存在: ${summ_dir}/.env"
    fi
}

_cmd_config() {
    local action="$1"
    shift 2>/dev/null || true

    case "$action" in
        get)
            [[ -z "$1" ]] && summ_error "用法: summ config get <KEY>"
            local val="$(summ_config_get "$1")"
            if [[ -n "$val" ]]; then
                echo "$val"
            else
                echo "(未设置)"
            fi
            ;;
        set)
            [[ -z "$1" || -z "$2" ]] && summ_error "用法: summ config set <KEY> <VALUE>"
            summ_config_set "$1" "$2"
            echo "已设置 $1"
            ;;
        *)
            summ_error "未知 config 操作: ${action:-空}。使用 get 或 set"
            ;;
    esac
}

# Route command
COMMAND="${1:-help}"
shift 2>/dev/null || true

case "$COMMAND" in
    help|init|list|config)
        "_cmd_${COMMAND}" "$@"
        ;;
    *)
        # Try plugin
        PLUGIN_FILE="${SUMM_HOME}/plugins/${COMMAND}/plugin.sh"
        [[ ! -f "$PLUGIN_FILE" ]] && summ_error "未知命令: ${COMMAND}，运行 summ list 查看可用命令"

        source "$PLUGIN_FILE"

        SUBCOMMAND="${1:-}"
        if [[ -n "$SUBCOMMAND" ]]; then
            shift
        fi

        if [[ -n "$SUBCOMMAND" ]] && declare -f "cmd_${SUBCOMMAND}" &>/dev/null; then
            local result
            result="$( "cmd_${SUBCOMMAND}" "$@" )" || exit $?
            summ_output "$result"
        elif declare -f cmd_default &>/dev/null; then
            local result
            result="$( cmd_default "$SUBCOMMAND" "$@" )" || exit $?
            summ_output "$result"
        else
            if [[ -n "$SUBCOMMAND" ]]; then
                summ_error "未知子命令: ${SUBCOMMAND}"
            else
                # No subcommand, no default — show help
                _cmd_help_plugin "$COMMAND"
            fi
        fi
        ;;
esac
```

- [ ] **Step 2: Make executable and verify syntax**

Run: `chmod +x bin/summ && bash -n bin/summ && echo "SYNTAX OK"`
Expected: `SYNTAX OK`

- [ ] **Step 3: Smoke test — help without plugins**

Run: `bin/summ help`
Expected: Shows "SUMM-Cli — 统一 API 工具集" header with usage instructions and empty plugin list.

- [ ] **Step 4: Smoke test — init**

Run: `bin/summ init`
Expected: Either "已创建配置文件: ~/.summ/.env" or "配置文件已存在".

- [ ] **Step 5: Commit**

```bash
git add bin/summ
git commit -m "feat: add CLI entry point with command routing"
```

---

### Task 4: Config template

**Files:**
- Create: `.env.example`

- [ ] **Step 1: Create `.env.example`**

```bash
# SUMM-Cli 配置文件
# 运行 summ config set <KEY> <VALUE> 或直接编辑此文件
# 环境变量会覆盖此文件中的值

# === API Keys ===
# CAIYUN_TOKEN=        # 彩云天气 API Token (https://www.caiyunapp.com/h5/)
# AMAP_API_KEY=        # 高德地图 API Key (https://console.amap.com/dev/key/app)

# === 默认值 ===
# DEFAULT_LONGITUDE=116.4
# DEFAULT_LATITUDE=39.9
```

- [ ] **Step 2: Commit**

```bash
git add .env.example
git commit -m "feat: add config template"
```

---

### Task 5: Weather plugin

**Files:**
- Create: `plugins/weather/plugin.sh`
- Create: `plugins/weather/SKILL.md`

Migrate weather.sh into the plugin convention.

- [ ] **Step 1: Create `plugins/weather/plugin.sh`**

```bash
#!/bin/bash
# plugins/weather/plugin.sh — 彩云天气查询

plugin_info() {
    echo "weather:彩云天气查询:realtime|daily|hourly|minutely"
}

plugin_help() {
    echo "用法: summ weather <子命令> [经度] [纬度]"
    echo ""
    echo "子命令:"
    echo "  realtime    实时天气"
    echo "  daily       逐日预报 (默认)"
    echo "  hourly      逐小时预报"
    echo "  minutely    分钟级降水"
    echo ""
    echo "环境变量 (或 summ config set):"
    echo "  CAIYUN_TOKEN        彩云天气 API Token (必需)"
    echo "  DEFAULT_LONGITUDE   经度 (默认 116.4)"
    echo "  DEFAULT_LATITUDE    纬度 (默认 39.9)"
    echo ""
    echo "示例:"
    echo "  summ weather daily"
    echo "  summ weather realtime 121.4 31.2"
    echo "  summ weather daily --json"
}

_weather_api_base() {
    echo "https://api.caiyunapp.com/v2.6"
}

_weather_request() {
    local endpoint="$1"
    local longitude="${2:-$(summ_config_get DEFAULT_LONGITUDE 116.4)}"
    local latitude="${3:-$(summ_config_get DEFAULT_LATITUDE 39.9)}"

    local token="$(summ_config_get CAIYUN_TOKEN)"
    [[ -z "$token" ]] && summ_error "未配置 CAIYUN_TOKEN，运行 summ config set CAIYUN_TOKEN <value>"

    local url="$(_weather_api_base)/${token}/${longitude},${latitude}/${endpoint}"
    curl -sf "$url" || summ_error "API 请求失败: $url"
}

cmd_realtime() { _weather_request "realtime" "$@"; }
cmd_daily()    { _weather_request "daily" "$@"; }
cmd_hourly()   { _weather_request "hourly" "$@"; }
cmd_minutely() { _weather_request "minutely" "$@"; }

cmd_default() { cmd_daily "$@"; }
```

- [ ] **Step 2: Verify syntax**

Run: `bash -n plugins/weather/plugin.sh && echo "SYNTAX OK"`
Expected: `SYNTAX OK`

- [ ] **Step 3: Create `plugins/weather/SKILL.md`**

```markdown
---
name: weather
description: Use when fetching weather data via Caiyun API, when user asks about weather, or needs weather info for reports
---

# Weather API

获取天气信息。

## 前提条件

1. 已运行 `/data/dev/SUMM-Cli/install.sh` 安装脚本
2. 设置 CAIYUN_TOKEN：`summ config set CAIYUN_TOKEN <token>`
3. 获取 Token: https://www.caiyunapp.com/h5/

## 快速参考

| 命令 | 用途 |
|------|------|
| `summ weather realtime` | 实时天气 |
| `summ weather daily` | 逐日预报 |
| `summ weather hourly` | 逐小时预报 |
| `summ weather minutely` | 分钟级降水 |

## 使用方法

```bash
# 今日天气预报
summ weather daily

# 实时天气
summ weather realtime

# 指定坐标
summ weather daily 121.4 31.2

# JSON 输出
summ weather daily --json
```

## 配置

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `CAIYUN_TOKEN` | API Token（必需） | - |
| `DEFAULT_LONGITUDE` | 经度 | 116.4（北京） |
| `DEFAULT_LATITUDE` | 纬度 | 39.9（北京） |

## 参考

- API文档: https://docs.caiyunapp.com/weather-api/v2/v2.6/4-daily.html
```

- [ ] **Step 4: Test plugin loads and shows info**

Run: `bin/summ help weather`
Expected: Shows weather plugin help with subcommand list.

- [ ] **Step 5: Test config-driven API call** (requires CAIYUN_TOKEN)

Run: `bin/summ config set CAIYUN_TOKEN test_token && bin/summ help weather`
Expected: Config saved, weather help displayed with all subcommands.

- [ ] **Step 6: Commit**

```bash
git add plugins/weather/
git commit -m "feat: add weather plugin (migrated from /data/app/base/weather)"
```

---

### Task 6: Amap-geo plugin

**Files:**
- Create: `plugins/amap-geo/plugin.sh`
- Create: `plugins/amap-geo/SKILL.md`

Migrate amap-geo.sh into the plugin convention.

- [ ] **Step 1: Create `plugins/amap-geo/plugin.sh`**

```bash
#!/bin/bash
# plugins/amap-geo/plugin.sh — 高德地图地理编码

plugin_info() {
    echo "amap-geo:高德地图地理编码:search|reverse"
}

plugin_help() {
    echo "用法: summ amap-geo <子命令> [选项]"
    echo ""
    echo "子命令:"
    echo "  search \"地址\" [-c 城市]   地理编码（地址 → 坐标）"
    echo "  reverse \"经度,纬度\"        逆地理编码（坐标 → 地址）"
    echo ""
    echo "选项:"
    echo "  -c, --city CITY   指定查询城市"
    echo ""
    echo "环境变量 (或 summ config set):"
    echo "  AMAP_API_KEY   高德地图 Web 服务 API Key (必需)"
    echo ""
    echo "示例:"
    echo "  summ amap-geo search \"天安门\" -c 北京"
    echo "  summ amap-geo reverse 116.397463,39.909187"
    echo "  summ amap-geo search \"天安门\" --json"
}

_urlencode() {
    jq -sRr @uri <<< "$1"
}

cmd_search() {
    local address=""
    local city=""
    local args=("$@")
    local i=0

    while [[ $i -lt ${#args[@]} ]]; do
        case "${args[$i]}" in
            -c|--city)
                city="${args[$((i+1))]}"
                ((i+=2))
                ;;
            *)
                address="${args[$i]}"
                ((i++))
                ;;
        esac
    done

    [[ -z "$address" ]] && summ_error "请输入地址"

    local api_key="$(summ_config_get AMAP_API_KEY)"
    [[ -z "$api_key" ]] && summ_error "未配置 AMAP_API_KEY，运行 summ config set AMAP_API_KEY <value>"

    command -v jq &>/dev/null || summ_error "需要安装 jq"

    local encoded_addr="$(_urlencode "$address")"
    local url="https://restapi.amap.com/v3/geocode/geo?key=${api_key}&address=${encoded_addr}&output=JSON"

    if [[ -n "$city" ]]; then
        local encoded_city="$(_urlencode "$city")"
        url="${url}&city=${encoded_city}"
    fi

    curl -sf "$url" || summ_error "API 请求失败: $url"
}

cmd_reverse() {
    local location="${1:-}"
    [[ -z "$location" ]] && summ_error "请输入坐标，格式: 经度,纬度"

    local api_key="$(summ_config_get AMAP_API_KEY)"
    [[ -z "$api_key" ]] && summ_error "未配置 AMAP_API_KEY，运行 summ config set AMAP_API_KEY <value>"

    local url="https://restapi.amap.com/v3/geocode/regeo?key=${api_key}&location=${location}&output=JSON"
    curl -sf "$url" || summ_error "API 请求失败: $url"
}

cmd_default() {
    if [[ -n "$1" ]]; then
        cmd_search "$@"
    else
        summ_error "请输入地址，例如: summ amap-geo search \"天安门\""
    fi
}
```

- [ ] **Step 2: Verify syntax**

Run: `bash -n plugins/amap-geo/plugin.sh && echo "SYNTAX OK"`
Expected: `SYNTAX OK`

- [ ] **Step 3: Create `plugins/amap-geo/SKILL.md`**

```markdown
---
name: amap-geocoding
description: Use when converting Chinese addresses to coordinates (longitude, latitude), when user mentions "地理编码", "地址转坐标", or needs location lookup for addresses in China
---

# AMap Geocoding (高德地图地理编码)

将中国地址转换为经纬度坐标。

## 前提条件

1. 已运行 `/data/dev/SUMM-Cli/install.sh` 安装脚本
2. 设置 AMAP_API_KEY：`summ config set AMAP_API_KEY <key>`
3. 申请地址: https://console.amap.com/dev/key/app (选择 "Web服务" 类型)

## 快速参考

| 命令 | 说明 |
|------|------|
| `summ amap-geo search "地址"` | 地理编码 |
| `summ amap-geo search "地址" -c 城市` | 指定城市 |
| `summ amap-geo reverse "经度,纬度"` | 逆地理编码 |

## 使用方法

```bash
# 地理编码
summ amap-geo search "天安门"
summ amap-geo search "天安门" -c 北京

# 逆地理编码
summ amap-geo reverse 116.397463,39.909187

# JSON 输出
summ amap-geo search "天安门" --json
```

## 配置

| 变量 | 说明 |
|------|------|
| `AMAP_API_KEY` | 高德地图 Web 服务 API Key（必需） |

## 参考

- API 文档: https://lbs.amap.com/api/webservice/guide/api/georegeo/
```

- [ ] **Step 4: Test plugin loads**

Run: `bin/summ help amap-geo`
Expected: Shows amap-geo help with search/reverse subcommands.

- [ ] **Step 5: Verify full plugin list**

Run: `bin/summ list`
Expected: Shows both `weather` and `amap-geo` plugins.

- [ ] **Step 6: Commit**

```bash
git add plugins/amap-geo/
git commit -m "feat: add amap-geo plugin (migrated from /data/app/base/amap-geo)"
```

---

### Task 7: Install script

**Files:**
- Create: `install.sh`

- [ ] **Step 1: Create `install.sh`**

```bash
#!/bin/bash
# install.sh — SUMM-Cli 系统安装器
# 幂等运行: 可多次执行，自动覆盖旧安装

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
INSTALL_DIR="/usr/local/bin"

# 获取实际用户的 HOME 目录（支持 sudo）
ACTUAL_HOME="${HOME}"
if [[ -n "$SUDO_USER" ]]; then
    ACTUAL_HOME="$(getent passwd "$SUDO_USER" | cut -d: -f6)"
fi
CLAUDE_SKILLS_DIR="$ACTUAL_HOME/.claude/skills"
SUMM_CONFIG_DIR="$ACTUAL_HOME/.summ"

echo "=== SUMM-Cli 安装 ==="

# 1. 安装 summ 命令
echo "[1/5] 安装 summ 到 $INSTALL_DIR"
mkdir -p "$INSTALL_DIR"
ln -sf "$SCRIPT_DIR/bin/summ" "$INSTALL_DIR/summ"
chmod +x "$SCRIPT_DIR/bin/summ"

# 2. 安装 TOON CLI
echo "[2/5] 检查 TOON CLI"
if ! command -v toon &>/dev/null; then
    if command -v npm &>/dev/null; then
        echo "       安装 @toon-format/cli..."
        npm install -g @toon-format/cli 2>/dev/null || echo "       安装失败，将使用 npx fallback"
    else
        echo "       未找到 npm，将使用 npx fallback"
    fi
else
    echo "       已安装: $(command -v toon)"
fi

# 3. 创建配置目录
echo "[3/5] 初始化配置目录"
mkdir -p "$SUMM_CONFIG_DIR"
if [[ ! -f "${SUMM_CONFIG_DIR}/.env" ]]; then
    cp "$SCRIPT_DIR/.env.example" "${SUMM_CONFIG_DIR}/.env"
    echo "       已创建配置文件: ${SUMM_CONFIG_DIR}/.env"
else
    echo "       配置文件已存在: ${SUMM_CONFIG_DIR}/.env"
fi

# 4. 安装 Claude Skills
echo "[4/5] 安装 Claude Skills"
for plugin_dir in "$SCRIPT_DIR"/plugins/*/; do
    [[ ! -f "${plugin_dir}SKILL.md" ]] && continue
    local_name="$(basename "$plugin_dir")"
    skill_target="${CLAUDE_SKILLS_DIR}/summ-${local_name}"
    mkdir -p "$skill_target"
    ln -sf "${plugin_dir}SKILL.md" "${skill_target}/SKILL.md"
    echo "       summ-${local_name} ✓"
done

# 5. 验证
echo "[5/5] 验证安装"
if command -v summ &>/dev/null; then
    echo ""
    summ help
    echo ""
    echo "安装成功: $(command -v summ)"
else
    echo "安装完成，但 summ 不在 PATH 中"
    echo "请将 $INSTALL_DIR 添加到 PATH"
fi
```

- [ ] **Step 2: Make executable and verify syntax**

Run: `chmod +x install.sh && bash -n install.sh && echo "SYNTAX OK"`
Expected: `SYNTAX OK`

- [ ] **Step 3: Test install (dry run)**

Run: `bash install.sh`
Expected: All 5 steps complete, `summ help` shows both plugins.

- [ ] **Step 4: Verify symlink works**

Run: `summ list`
Expected: Shows `weather` and `amap-geo` plugins.

- [ ] **Step 5: Commit**

```bash
git add install.sh
git commit -m "feat: add system installer script"
```

---

### Task 8: End-to-end verification

**Files:**
- No new files

Verify the entire system works as a cohesive unit.

- [ ] **Step 1: Test built-in commands**

Run:
```bash
summ help && echo "---OK1---"
summ list && echo "---OK2---"
summ help weather && echo "---OK3---"
summ help amap-geo && echo "---OK4---"
summ config get DEFAULT_LONGITUDE && echo "---OK5---"
```
Expected: All commands succeed with OK markers.

- [ ] **Step 2: Test config management**

Run:
```bash
summ config set TEST_KEY test_value && \
summ config get TEST_KEY && \
summ config get NONEXISTENT || echo "---OK6---"
```
Expected: `test_value`, then `(未设置)`, then OK6.

- [ ] **Step 3: Test --json flag**

Run:
```bash
summ config set CAIYUN_TOKEN dummy_token
summ weather daily --json 2>&1 | head -5 || echo "---Expected API error OK---"
```
Expected: Either JSON output (if token valid) or error about API request failure (token is dummy).

- [ ] **Step 4: Test unknown command**

Run: `summ nonexistent 2>&1; echo "exit: $?"`
Expected: Error message containing "未知命令", exit code 1.

- [ ] **Step 5: Test plugin without subcommand**

Run: `summ weather 2>&1`
Expected: Shows weather help info.

- [ ] **Step 6: Final commit — add README**

Create `README.md`:

```markdown
# SUMM-Cli

统一 API 工具集 CLI。

## 安装

```bash
sudo ./install.sh
```

## 使用

```bash
summ list                  # 查看可用插件
summ help <插件>           # 查看插件帮助
summ init                  # 初始化配置
summ config set KEY VALUE  # 设置配置
```

## 添加新插件

在 `plugins/<name>/` 下创建:

1. `plugin.sh` — 实现 `plugin_info()` 和 `cmd_<子命令>()` 函数
2. `SKILL.md` — Claude Agent Skill 定义

无需修改核心代码，自动发现。

## 配置

配置文件: `~/.summ/.env`

环境变量优先于配置文件。
```

```bash
git add README.md
git commit -m "docs: add README"
```

---

## Self-Review Checklist

- [x] **Spec coverage:** Every section of the design spec maps to a task. Config (Task 1,4), output (Task 2), routing (Task 3), weather plugin (Task 5), amap-geo plugin (Task 6), install (Task 7), E2E (Task 8).
- [x] **No placeholders:** All code blocks contain complete implementations. No TBD/TODO.
- [x] **Type consistency:** Function names (`summ_config_get`, `summ_output`, `summ_error`, `plugin_info`, `cmd_*`) used consistently across all tasks.
- [x] **File paths exact:** Every task specifies exact file paths.
- [x] **Verification commands exact:** Every step has a run command and expected output.
